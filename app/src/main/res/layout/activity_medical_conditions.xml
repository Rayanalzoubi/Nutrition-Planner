<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/medical_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:paddingStart="24dp"
    android:paddingEnd="24dp">

    <!-- 1) Back arrow -->
    <ImageView
        android:id="@+id/ivBackMedical"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_arrow_back"
        android:contentDescription="@string/back"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

    <!-- 2) Logo -->
    <ImageView
        android:id="@+id/imgLogoMedical"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/logonew"
        android:contentDescription="@string/app_name"
        android:layout_marginTop="0dp"
        app:layout_constraintTop_toBottomOf="@id/ivBackMedical"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 3) Title -->
    <TextView
        android:id="@+id/tvTitleMedical"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your Medical Conditions"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#000"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/imgLogoMedical"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 4) Illustration -->
    <ImageView
        android:id="@+id/imgIllustrationMedical"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:src="@drawable/medicalpic"
        android:scaleType="centerCrop"
        android:contentDescription="@null"
        android:layout_marginTop="16dp"
        app:layout_constraintWidth_percent="0.9"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintTop_toBottomOf="@id/tvTitleMedical"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 5) “why we ask” -->
    <TextView
        android:id="@+id/tvWhyMedical"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/why_we_ask"
        android:textSize="16sp"
        android:textColor="#2ECC71"
        android:clickable="true"
        android:focusable="true"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/imgIllustrationMedical"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 6) Checkboxes -->
    <LinearLayout
        android:id="@+id/llConditions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/tvWhyMedical"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <CheckBox
            style="@style/ChoiceCheck"
            android:id="@+id/checkHeart"
            android:text="@string/cond_heart"/>

        <CheckBox
            style="@style/ChoiceCheck"
            android:id="@+id/checkDiabetes"
            android:text="@string/cond_diabetes"
            android:layout_marginTop="8dp"/>

        <CheckBox
            style="@style/ChoiceCheck"
            android:id="@+id/checkHypertension"
            android:text="@string/cond_hypertension"
            android:layout_marginTop="8dp"/>
    </LinearLayout>

    <!-- 7) Next button -->
    <Button
        android:id="@+id/btnNextMedical"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:text="Next"
        android:textAllCaps="false"
        android:background="@drawable/btn_pill_green"
        android:textColor="#000"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@id/llConditions"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>
