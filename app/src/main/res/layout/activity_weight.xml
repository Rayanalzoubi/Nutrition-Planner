<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/weight_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:paddingStart="24dp"
    android:paddingEnd="24dp">

    <!-- Back arrow -->
    <ImageView
        android:id="@+id/ivBackWeight"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_arrow_back"
        android:contentDescription="@string/back"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="16dp"/>

    <!-- Logo -->
    <ImageView
        android:id="@+id/imgLogoWeight"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/logonew"
        android:contentDescription="@string/app_name"
        app:layout_constraintTop_toBottomOf="@id/ivBackWeight"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp"/>

    <!-- Title -->
    <TextView
        android:id="@+id/tvTitleWeight"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="what’s your Weight?"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#000"
        app:layout_constraintTop_toBottomOf="@id/imgLogoWeight"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"/>

    <!-- Illustration -->
    <ImageView
        android:id="@+id/imgIllustrationWeight"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:src="@drawable/weightpic"
        android:scaleType="centerCrop"
        app:layout_constraintWidth_percent="0.9"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvTitleWeight"
        android:layout_marginTop="16dp"/>

    <!-- why we ask -->
    <TextView
        android:id="@+id/tvWhyWeight"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/why_we_ask"
        android:textSize="16sp"
        android:textColor="#2ECC71"
        app:layout_constraintTop_toBottomOf="@id/imgIllustrationWeight"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="12dp"/>

    <!-- weight entry + underline + suffix -->
    <!-- 6) Weight entry (packed horizontal chain) -->
    <EditText
        android:id="@+id/editTextWeightOnly"
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:inputType="numberDecimal"
        android:layout_marginRight="10dp"
        android:textSize="24sp"
        android:background="@android:color/transparent"
        android:importantForAutofill="yes"
        android:gravity="center"
        android:padding="0dp"
        app:layout_constraintTop_toBottomOf="@id/tvWhyWeight"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/tvKgSuffix"
        app:layout_constraintHorizontal_chainStyle="packed"/>

    <TextView
        android:id="@+id/tvKgSuffix"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Kg"
        android:textSize="24sp"
        android:textColor="#000"
        app:layout_constraintBaseline_toBaselineOf="@id/editTextWeightOnly"
        app:layout_constraintStart_toEndOf="@id/editTextWeightOnly"
        app:layout_constraintEnd_toEndOf="parent"/>

    <View
        android:id="@+id/underlineWeight"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="#000"
        app:layout_constraintTop_toBottomOf="@id/editTextWeightOnly"
        app:layout_constraintStart_toStartOf="@id/editTextWeightOnly"
        app:layout_constraintEnd_toEndOf="@id/tvKgSuffix"/>

    <!-- 7) Next button -->
    <Button
        android:id="@+id/btnNextWeight"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_marginTop="32dp"
        android:background="@drawable/btn_pill_green"
        android:gravity="center"
        android:text="Next"
        android:textAllCaps="false"
        android:textColor="#000"
        app:layout_constraintTop_toBottomOf="@id/underlineWeight"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
